# Smart Leads Dashboard Client

React + TypeScript + TailwindCSS frontend for the Smart Leads Dashboard.

## Commands

```bash
npm install
npm run dev
npm run build
```

Set `VITE_API_URL` in `.env`:

```env
VITE_API_URL=http://localhost:5000/api
```
